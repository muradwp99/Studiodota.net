# Claude Code local config & scratch
.claude/

# Installed agent skills (third-party, reinstallable)
.agents/

# Dependencies / build (belt-and-suspenders; web/ has its own too)
node_modules/
.next/
out/
dist/

# Raw hero-frame source (6.9 GB) — optimized frames live in web/public/media/hero-seq/
Homepage_ref/

# Logs & OS
*.log
.DS_Store
Thumbs.db
.mysql/
