# Claude Code local config & scratch
.claude/

# Installed agent skills (third-party, reinstallable)
.agents/

# Dependencies / build (belt-and-suspenders; web/ has its own too)
node_modules/
.next/
out/
dist/

# Raw hero-frame source (6.9 GB) — optimized frames live in web/public/media/hero-seq/
Homepage_ref/

# Client's completed-projects delivery (1.5 GB of full-res originals) — the
# optimized webp derivatives live in web/public/projects/ and ARE committed.
# See scripts/import-completed-projects.mjs for the curation.
Projects/

# Logs & OS
*.log
.DS_Store
Thumbs.db
.mysql/

# Magnific hero renders (masters + clips, ~100 MB) — the 300-frame webp
# sequence derived from them lives in web/public/media/hero-seq-v3/ and IS
# committed. Keep the masters locally so the sequence can be rebuilt.
hero-ball-residence/
